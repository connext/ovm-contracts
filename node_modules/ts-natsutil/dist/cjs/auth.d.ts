import { ILogger } from '.';
export declare class AuthService {
    private audience;
    private log?;
    private privateKey;
    private publicKey;
    constructor(log: ILogger, audience: string, privateKey: string, publicKey: string);
    vendBearerJWT(subject: string, ttl: number, permissions: any): Promise<string>;
    verifyBearerJWT(token: string): boolean;
}
//# sourceMappingURL=auth.d.ts.map