/// <reference types="node" />
import { Sub } from './nats';
import { EventEmitter } from 'events';
/**
 * @hidden
 */
export declare class Subscriptions extends EventEmitter {
    mux: Sub;
    subs: {
        [key: number]: Sub;
    };
    sidCounter: number;
    length: number;
    constructor();
    add(s: Sub): Sub;
    setMux(s: Sub): Sub;
    getMux(): Sub | null;
    get(sid: number): Sub | null;
    all(): Sub[];
    cancel(s: Sub): void;
    close(): void;
}
