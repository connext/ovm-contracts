/// <reference types="node" />
import Timer = NodeJS.Timer;
export interface RequestConfiguration {
    token: string;
    callback: Function;
    inbox: string;
    id: number;
    received: number;
    expected?: number;
    timeout?: Timer;
}
