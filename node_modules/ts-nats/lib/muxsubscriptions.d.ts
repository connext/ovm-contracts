import { Msg, Req } from './nats';
import { NatsError } from './error';
/**
 * @hidden
 */
export declare class MuxSubscriptions {
    baseInbox: string;
    reqs: {
        [key: string]: Req;
    };
    length: number;
    init(): string;
    add(r: Req): void;
    get(token: string): Req | null;
    all(): Req[];
    cancel(r: Req): void;
    close(): void;
    getToken(m?: Msg): string | null;
    dispatcher(): (error: NatsError | null, m: Msg) => void;
}
