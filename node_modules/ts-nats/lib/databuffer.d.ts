/// <reference types="node" />
/**
 * @hidden
 */
export declare class DataBuffer {
    buffers: Buffer[];
    byteLength: number;
    pack(): void;
    drain(n?: number): Buffer;
    fill(data: Buffer): void;
    protoLen(): number;
    peek(): Buffer;
    size(): number;
    length(): number;
    reset(): Buffer[];
}
