import { UrlString, IChannelSigner, PublicIdentifier, PublicKey, PrivateKey, HexString, SignatureString, Address } from "@connext/vector-types";
import { Provider, TransactionResponse, TransactionRequest } from "@ethersproject/abstract-provider";
import { Signer } from "@ethersproject/abstract-signer";
export declare const getRandomChannelSigner: (provider?: UrlString | Provider) => ChannelSigner;
export declare class ChannelSigner extends Signer implements IChannelSigner {
    private readonly privateKey;
    address: Address;
    publicIdentifier: PublicIdentifier;
    publicKey: PublicKey;
    provider?: Provider;
    private readonly _ethersType;
    constructor(privateKey: PrivateKey, provider?: UrlString | Provider);
    getAddress(): Promise<Address>;
    encrypt: (message: string, publicKey: string) => Promise<string>;
    connectProvider(provider?: UrlString | Provider): Promise<void>;
    connect(provider: Provider): ChannelSigner;
    decrypt(message: string): Promise<HexString>;
    signMessage(message: string): Promise<SignatureString>;
    signUtilityMessage(message: string): Promise<SignatureString>;
    signTransaction(transaction: TransactionRequest): Promise<string>;
    sendTransaction(transaction: TransactionRequest): Promise<TransactionResponse>;
}
//# sourceMappingURL=channelSigner.d.ts.map